function friend(friends){
  return friends.filter(name => name.length === 4);
}

/* I got help from Claude making sure I understood and was writing this smaller-than-I'm-used-to-doing function. I had to make sure using "name =>" worked. I still need more familiarity with it, but I understand it. */