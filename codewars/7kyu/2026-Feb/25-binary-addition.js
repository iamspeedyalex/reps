function addBinary(a,b) {
  let sum = a + b;
  return sum.toString(2)
  
  /*  - add the two numbers together
      - convert the sum to binary (base 2)
      - return as a string */

}

/* .toString(2) converts a number to binary string. This is interesting. I didn't know this could be done. */