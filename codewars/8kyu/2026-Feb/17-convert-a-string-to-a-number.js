const stringToNumber = function(str){
  let result = Number(str);
  
  return result;
}

/* Claude helped me understand that the "result null;" in the given code at the start was using null just as a placeholder. I learned null is reserved already, and I simply needed to use my own variable name for it. */