function check(a, x) {
  let result = a.includes(x);
  return result;
}

/* My answer after Claude helped remind me about .includes() */

function check(a, x) {
  return a.includes(x);
}

/* Claude showed me this clean one-liner. I wanted to write it here so there's a better chance I remember it by having written it before. */